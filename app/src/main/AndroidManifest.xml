<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />

    <!-- Needed to bind the HyperOS pen service for Focus Pen barrel gestures (Android 11+ package visibility). -->
    <queries>
        <package android:name="com.xiaomi.touchservice" />
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.DrawAnywhere">

        <activity
            android:name="androidx.activity.ComponentActivity"
            android:exported="false" />

        <service
            android:name=".DrawAnywhereTileService"
            android:label="DrawAnywhere"
            android:icon="@drawable/ic_tile_draw"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.DrawAnywhere.TransparentActivity"
            android:excludeFromRecents="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.DrawAnywhere" />

        <activity
            android:name=".FocusPenTestActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.DrawAnywhere" />

        <activity
            android:name=".ScreenCapturePermissionActivity"
            android:exported="false"
            android:theme="@style/Theme.DrawAnywhere.TransparentActivity"
            android:excludeFromRecents="true"
            android:noHistory="true" />

        <activity
            android:name=".SaveLocationPickerActivity"
            android:exported="false"
            android:theme="@style/Theme.DrawAnywhere.TransparentActivity"
            android:excludeFromRecents="true"
            android:noHistory="true" />

        <service android:name=".MainService"
            android:exported="false"
            android:foregroundServiceType="specialUse|mediaProjection">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="The foreground service is used to host the canvas on top of other apps, which is the business logic of this application." />
        </service>

    </application>

</manifest>
